"use strict"

//Ecrivez un programme qui affiche tous les nombres de 100 à 0 avec un interval de 2 entre chaque nombre.

//compteur de base à 100

for ( let compteur = 100 ; compteur >= 0 ; compteur -= 2 ) {
    console.log(compteur)
}

